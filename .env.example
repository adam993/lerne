# Lerne — env vars reference. Copy to .env.local if anything actually needs
# configuring at build time.
#
# The app is fully offline. There are currently no required env vars. This
# file is kept as a placeholder so future contributors know to look here
# first when adding any external integration.
